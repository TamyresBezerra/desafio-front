// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  configFirebase: {
    apiKey: "AIzaSyBEQMXbtkDV3TIZvVNo307THriz9sl2VeA",
    authDomain: "cliente-oculto-d5901.firebaseapp.com",
    databaseURL: "https://cliente-oculto-d5901.firebaseio.com",
    projectId: "cliente-oculto-d5901",
    storageBucket: "cliente-oculto-d5901.appspot.com",
    messagingSenderId: "1067508396429"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
